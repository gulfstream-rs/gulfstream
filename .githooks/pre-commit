#!/usr/bin/env bash
set -euo pipefail

export RUSTFLAGS="-Dwarnings"
export RUSTDOCFLAGS="-Dwarnings"

echo "[pre-commit] Validating dashboard assets..."
node scripts/validate-web.mjs

echo "[pre-commit] Checking cargo fmt..."
cargo fmt --all -- --check

echo "[pre-commit] Running cargo check..."
cargo check --all-targets --all-features

echo "[pre-commit] Running cargo clippy..."
cargo clippy --all-targets --all-features -- -D warnings

echo "[pre-commit] Running cargo test..."
cargo test --all-targets --all-features

echo "[pre-commit] OK"
